spring:
  datasource:
    password: root
    username: root
    url: jdbc:h2:mem:blog
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
